# Core
Django>=5.1,<5.2
djangorestframework>=3.15
django-tenants>=3.7
psycopg[binary]>=3.2
django-environ>=0.11
django-cors-headers>=4.4

# Async / queue
celery>=5.4
redis>=5.0
django-redis>=5.4

# Storage (MinIO / S3)
django-storages>=1.14
boto3>=1.35

# Media
Pillow>=10.4

# Exports (XLSX)
openpyxl>=3.1

# Auth (Auth0 JWT validation)
pyjwt[crypto]>=2.9

# Payments (Razorpay gateway + webhook reconciliation)
razorpay>=1.4

# Monitoring
sentry-sdk>=2.13

# Server
gunicorn>=23.0
whitenoise>=6.7   # serve static files (incl. Django admin) under gunicorn

# Dev / test
pytest>=8.3
pytest-django>=4.9
ruff>=0.6
black>=24.8
mypy>=1.11
